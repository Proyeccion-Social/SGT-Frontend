---
import Layout from "@/layouts/Layout.astro";
import Sidebar from "@/features/general/components/Sidebar.astro";
import VaulDrawer from "@/components/ui/drawer";

interface Props {
  title?: string;
  rol: "admin" | "tutor" | "student";
}

const { rol, title = "Atlas" } = Astro.props;

const token = Astro.cookies.get("access_token")?.value;
---

<Layout title={title}>
  <div class='dashboardContainer'>
    <Sidebar userRole={rol} transition:persist />
    <slot />
  </div>
  {rol === "tutor" && <VaulDrawer client:load transition:persist />}
</Layout>

<style>
  .dashboardContainer {
    display: flex;
    width: 100%;
    height: 100vh;
    background-color: #f3edff;
    padding: 18px 20px 20px 0;
    box-sizing: border-box;
    overflow-y: hidden;
  }
</style>
